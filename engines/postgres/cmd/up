#!/usr/bin/env bash
set -euo pipefail
source "${LIBRARY_ROOT}/engine-lib.sh"

ensure_network
( cd "${ENGINE_ROOT}/postgres"
    PG_VER="${PG_VER:-${POSTGRES_VER_DEFAULT:-16}}" \
    compose -f compose.yaml --profile engine up -d
)

